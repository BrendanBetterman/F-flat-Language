//  12IfThen.flat
//
//  Tests simple fif statements (no else clauses).

BOF

	int n, m;
	
	fout( "Enter two integers: ");
	fin( n, m);
	fif ( n > m )
		foutln( n, " is greater than ", m, "\n");
	fendif
	fif ( m > n )
		foutln( m, " is greater than ", n, "\n");
	fendif
	foutln( "\n");

EOF
