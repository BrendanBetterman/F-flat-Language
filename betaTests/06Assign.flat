/* 06Assign.flat

   Arithmetic expressions and assignments.
*/ 

BOF

	int  x;
	fake y, z;
	int  n;

	x = ((3 + 5) * 4);
	fout( "\nx =", x);
	y = (2.3 + 7.1) * 5.0);
	fout( "\ny =", y);
	z = 5.0 * (3.6 - 2.9));
	fout( "\nz =", z);
	n = ((2 + 6 * 9) - 8) / 7);
	fout( "\nn =", n); 
EOF
